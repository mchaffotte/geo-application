import { InputFocusDirective } from './input-focus.directive';

describe('AutoFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFocusDirective(null);
    expect(directive).toBeTruthy();
  });
});
